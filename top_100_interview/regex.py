class Solution:

    # a much much dumbed down version of regex
    # i couldn't find a way to compile it into an NFA (https://swtch.com/~rsc/regexp/regexp1.html) on my own. 
    def isMatch(self, s: str, p: str) -> bool:
        pass 